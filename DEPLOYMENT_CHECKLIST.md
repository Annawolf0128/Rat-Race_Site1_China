# 正式实验启动与现场验收

程序修复和本地验收不能代替实际服务器、15 台被试电脑的现场联机检查。
本地测试使用 Python 3.9、oTree 5.11.5、PostgreSQL 16；依赖固定在 requirements.txt。

## 启动

在最终部署目录创建虚拟环境，安装 `python -m pip install -r requirements.txt`。
先由实验室配置专用 PostgreSQL 数据库；不要把测试数据库当作正式数据库，也不要删除已有数据。

设置这三个环境变量（请使用实验室自己的值）：

- `DATABASE_URL`：`postgresql://数据库用户:密码@数据库地址:5432/数据库名`
- `OTREE_ADMIN_PASSWORD`：后台管理员密码
- `OTREE_SECRET_KEY`：独立随机字符串，正常重启保持不变

虚拟环境激活后运行：

```text
python scripts/start_study.py --port 8000
```

脚本自动设置 `OTREE_PRODUCTION=1` 和 `OTREE_AUTH_LEVEL=STUDY`。
正式模式下使用 SQLite 将被拒绝。PostgreSQL 需先启动且能连接。
后台登录后使用 Rooms / Sessions，参加者使用 Seat01–Seat15 的 Room 书签。
不要在选择、预测、理解题、问卷阶段代填或强制推进。开始按钮仅在 15 人全部到 Welcome 时启用。
macOS 启动及停止链已测试；实际服务器若为其他系统，必须在该系统复验。

## 现场必须逐项确认（以下需要真实实验环境，不能用本地测试自动勾选）

- [ ] 服务器部署的是验收后的同一提交；requirements 安装完成，无版本漂移。
- [ ] 确认实际使用 PostgreSQL；按正式启动命令启动，后台有登录保护，被试页面无 Debug info。
- [ ] 15 台电脑分别使用唯一 Seat01–Seat15 书签，连接同一服务器；Room 显示 15 个不同座位。
- [ ] 六个处理在这套服务器/网络上演练；至少完成预定正式规模的完整场次，不只测单人页面。
- [ ] 现场共同点击提交，等待页正确；一台电脑迟交时同组等待，其他小组独立继续。
- [ ] 关闭一台被试浏览器、重新打开原座位书签，恢复同一身份和进度，无重复成员。
- [ ] 用已知选择核对结果、Payment 与后台；确认付款方式如何处理人民币分位金额。
- [ ] CSV 与 Page times 下载可用，文件能打开；付款轮次、选择、问卷、金额完整。
- [ ] 数据库备份完成并演练恢复到另一个测试数据库；确认不会覆盖正式数据。
- [ ] 服务器不会在实验期间休眠；网络、供电、备用联系方式及中断处理负责人明确。

## 每场开场

- [ ] 按场次表选择正确处理、15 人；不要把测试场次计入正式场次。
- [ ] 全部到 Welcome 后统一宣读说明，点“开始实验（仅欢迎页）”。
- [ ] 观察各组正常推进；遇到掉线先用原书签重进，不创建替代场次、不 reset 数据库。
- [ ] 全员 Payment 后导出 CSV 和 Page times，核对后台付款表并备份。

预定场次安排保持原设计：small_low 1 场、small_high 1 场、large_low 3 场、large_high 3 场、small_low_belief 1 场、large_low_belief 1 场，共 10 场。
分组仍按等待池形成并固定 20 轮；本次没有改为随机分组，也没有增加预测奖金或缺失选择代填规则。

## 已有数据和中断

付款精度修复不自动纠正旧场次已舍入的标准 payoff。旧场次应从原始 round_payoff 和 paid_round 另行对账。
有人退出且不能回来时，没有自动补位规则，该组会继续等待；不要把“等不到人”当作可以填 0 的理由。
是否终止、如何补偿由研究方案决定并记录。

重启前尽可能先导出 Page times，它包含框架内存缓冲的时间记录；突然断电可能丢失未落盘的少量页面时间。
主选择和问卷数据在 PostgreSQL 中按事务保存；这不等于已经验证实际机器突然断电的所有后果。
