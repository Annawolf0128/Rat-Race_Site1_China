# 中国站付款与滑块修复（2026-09-09）

基于主分支 `3f31e506b9ec2f2a564b25779c10fbb89500604a`，本地分支 `fix/payment-slider`。

- 点数保留三位小数：整数选择的成本 x²/200 最小增量为 0.005 点。
- 问卷提交后结算抽中轮次，先转为精确 Currency 再赋值；付款页面不再写入 payoff。
- 付款页面与后台统一调用 oTree 的现金换算、出场费合计方法，人民币显示两位小数。
- 滑块使用显式提交按钮，未选择时隐藏且禁用；点击初始值 50 也算明确选择；空提交不会触发 oTree 的 15 秒按钮锁。
- 正式处理、分组、抽签及严格低于中位数才扣罚金的规则保持原设计。

## 已有场次

此修复验证的是新建场次。已经进入付款页的旧场次不会自动重算；旧数据可能已经被整数精度污染。
应用到已有数据前，应备份并从原始 `round_payoff`、`paid_round` 对账，不能只根据旧 `participant.payoff` 补小数。
不要删除或 reset 正式数据库。此次没有连接、修改或部署到正式服务器，也没有推送远端。

## 并发检查尚未通过正式验收

六种处理的 15 人同步提交、共 1,800 次选择通过；但混合页面停留/后台任务场景复现 SQLite 数据库锁及 HTTP 500。
本地安装的 oTree 5.11.5 后台 worker 在数据库事务内访问页面，日志显示其活动与锁冲突重合；
尚未完成数据库锁的逐事务归因，也未验证实际服务器使用何种数据库。

建议在正式服务器的 PostgreSQL 环境重新测试。oTree 维护者也明确指出正式运行应使用 PostgreSQL：
https://groups.google.com/g/otree/c/RcJ5Kz8U6gE

测试方法见 [tests/README.md](tests/README.md)。
